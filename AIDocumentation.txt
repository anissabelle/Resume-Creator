I used both Codex and ChatGPT to assist me with development.

Codex created my SQL Database and API routes.
I also used Codex to debug and run end-to-end tests for creating an account,
logging in, and creating the resume.

I had some issues with running my node server. Codex recognized there was an
old process running on Port 8000 so I had it change my HTTP_PORT to 8001.

I had Codex alter my Swal.fire updates after I found errors with the popups not
popping up. It was easier to have codex alter the same copied and pasted lines
of code rather than alter every instance of that code block.

Another error is handled was my /title API route/request failing. Codex used the DatabaseSync
library for creating/executing SQL statements and used the .transaction function.
It found that this function did not exist in the library it included so I had Codex change
the sql statements to use explicit sql commands for creating a transaction.

I also had Codex and ChatGPT help with implementing the AI feature. Codex helped
troubleshoot the AI call to ensure it was working.

Also asked Codex how I can display the completed resume draft to take up most the screen.
It used Bootstrap columns which I figured would be the solution.

I used ChatGPT primarily to inspect code written by Codex so I could understand it efficiently.
I copied and pasted certain lines I was unfamiliar with to understand how the code worked
and I added comments to show my understanding. Most of the comments I created myself were
the single line comments.